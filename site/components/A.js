import styled from '@emotion/styled'
import theme from './theme'

const A = styled.a`
  color: inherit;
  text-decoration: none;
  padding-bottom: 2px;
  border-bottom: 0.5px solid ${theme.colors.neutral.medium};
`
export default A
